name: Echo only
on: 
 push: 
   branches:
   - main
env: 
  APP_NAME: "jfld"
  VERSION: "4.67"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Echo the command
      run: echo "we are building the application ${{.env.APP_NAME}} and version ${{env.VERSION}}"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploying to k8s
        run: echo "deploying to k8s"
   
